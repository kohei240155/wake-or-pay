ドキュメントは全て日本語で作成し、回答も日本語にして。

## Active Technologies
- TypeScript 5.x (strict), Node.js 20 (Vercel default) + Next.js 15 (App Router), React 19, Tailwind CSS, `@upstash/qstash`（署名検証・スケジュール作成/キャンセル用公式SDK）, `openai` 公式 SDK (001-wake-or-pay)
- なし（サーバー側 DB 不使用）。ブラウザ `localStorage` のみ（Alarm 状態・QStash スケジュールID の表示用キャッシュ） (001-wake-or-pay)

## Recent Changes
- 001-wake-or-pay: Added TypeScript 5.x (strict), Node.js 20 (Vercel default) + Next.js 15 (App Router), React 19, Tailwind CSS, `@upstash/qstash`（署名検証・スケジュール作成/キャンセル用公式SDK）, `openai` 公式 SDK
